server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:          uri: https://github.com/msoysal92/spring-cloud-config-repo
          searchPaths: licensingservice,organizationservice
